# @biocontext/shared

Shared types and helpers used by `apps/tui` and `packages/engine`.

- `thread.ts`: conversation message and chunk types, history formatting
- `stream-filter.ts`: strips echoed prompts and internal tags out of streamed answers
- `jsonc.ts`: JSONC parsing for `biocontext.config.jsonc`

This is an internal workspace package.
